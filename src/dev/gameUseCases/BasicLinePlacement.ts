import type { UseCase } from './types';

export const basicLinePlacement: UseCase = {
  name: 'Basic Line Placement',
  description: 'Simple 2-card line',
  gameState: {
    // Placeholder - will be populated with actual GameState
    phase: 'playing',
  },
};

