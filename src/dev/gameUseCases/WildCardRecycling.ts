import type { UseCase } from './types';

export const wildCardRecycling: UseCase = {
  name: 'Wild Card Recycling',
  description: 'Replacing wild card scenario',
  gameState: { phase: 'playing' },
};

